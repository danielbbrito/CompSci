#include <bits/stdc++.h>

using namespace std;

int main()
{
    int c;
    cin >> c;

    while (c--)
    {
        int deg;
        cin >> deg

        vector<int> coef;

        for (int i = 0; i <= deg; i++)
        {
            int item;
            cin >> item
            coef.push_back(item)
        }

        int d, k;
        cin >> d >> k;

        
    }
}