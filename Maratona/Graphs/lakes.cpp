#include <bits/stdc++.h>

using namespace std;
char grid[99][99];

int main()
{
    int m;
    cin >> m;
    cin.ignore();

    string line;
    getline(cin, line);

    while (m--)
    {
        string next = line;
        line = "";

        getline(cin, line);

        while (line.find(" ") == string::npos && line != "\0")
        {
            for (i)
        }
    }
}